# Output

This directory is reserved for generated dashboard output and deployment-relevant files.

## Expected artifacts

- `index.html` — final single-file dashboard output
- any generated JSON or summary snapshots used for verification
- deployment notes for hosting on a static or intranet server

## Governance note

The output is treated as a governed artifact because it embeds all office-level values and needs to be hosted carefully.
